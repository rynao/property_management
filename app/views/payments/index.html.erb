<%= render 'layouts/header' %>

<div class='container'>
<h4 class='py-3'>家賃管理</h4>
<%=link_to '支払い登録', new_payment_path, class:'btn btn-primary' %>
<%= column_chart @chart %>
  <table class='table mt-3'>
    <thead>
      <tr>
        <th><%= link_to "物件名称", :sort => "building"%></th>
        <th><%= link_to "期間", :sort => "paid_date"%></th>
        <th><%= link_to "合計家賃", :sort => "rent"%></th>
      </tr>
    </thead>
    <tbody>
      <% @summary.each do |smr| %>
        <tr>
          <td><%= smr['building'] %></td>
          <td><%= smr['paid_date'].strftime("%Y年%m月") %></td>
          <td><%= smr['sumrent'].to_s(:currency, locale: 'jp') %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
